{
  "name": "svef.is",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "false": "0.0.4",
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "devDependencies": {
    "@kadira/storybook": "^2.21.0",
    "@kadira/storybook-addon-options": "^1.0.1",
    "react-scripts": "0.9.3",
    "supervisor": "^0.12.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "storybook": "supervisor --no-restart-on exit --quiet --watch .storybook --exec npm -- run start-storybook --silent",
    "start-storybook": "start-storybook --port 9009 --static-dir public --config-dir .storybook",
    "build-storybook": "build-storybook --static-dir public --config-dir .storybook --output-dir build-storybook"
  }
}
